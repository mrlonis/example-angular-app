import { IFrameResizer } from './iframe-resizer';

describe('IFrameResizer', () => {
  it('should create an instance', () => {
    const directive = new IFrameResizer();
    expect(directive).toBeTruthy();
  });
});
